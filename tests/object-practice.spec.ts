import { test } from '@playwright/test';

test('Object Practice', async () => {

    const employee = {
        name: 'Srinivas',
        age: 25
    };

    console.log(employee.name);
    console.log(employee.age);

});